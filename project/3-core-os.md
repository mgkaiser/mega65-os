# 3 - Core OS

29 work items imported from `MEGA65_OS_User_Stories.xlsx`.

| ID | Epic | Priority | Status | Estimate | Dependencies | Chapters | Work item | Acceptance criteria |
|---|---|---|---|---|---|---|---|---|
| US-023 | Kernel Objects | Must | Backlog | L | US-020 | 9 | As a project contributor, I want to implement process-scoped handles so that the MEGA65 OS architecture can be implemented incrementally. | applications use validated handles rather than kernel pointers |
| US-024 | Kernel Objects | Must | Backlog | M | US-023 | 9 | As a project contributor, I want to separate ownership from references so that the MEGA65 OS architecture can be implemented incrementally. | shared object lifetime remains correct when original owner exits |
| US-025 | Kernel Objects | Must | Backlog | L | US-023, US-024 | 9 | As a project contributor, I want to track per-process owned resources so that the MEGA65 OS architecture can be implemented incrementally. | forced termination reclaims resources without global PID scans |
| US-026 | Signals | Must | Backlog | L | US-020 | 7,10 | As a project contributor, I want to implement process signals including uncatchable kill so that the MEGA65 OS architecture can be implemented incrementally. | catchable signals work and forced kill executes no further target instructions |
| US-027 | IPC | Must | Backlog | L | US-023 | 10 | As a project contributor, I want to implement message ports so that the MEGA65 OS architecture can be implemented incrementally. | cross-process send/receive/reply supports blocking and nonblocking operation |
| US-028 | IPC | Must | Backlog | M | US-023, US-011 | 10 | As a project contributor, I want to transfer shared-memory handles so that the MEGA65 OS architecture can be implemented incrementally. | processes share one object with independently controlled permissions |
| US-029 | IPC | Must | Backlog | M | US-023 | 10,19 | As a project contributor, I want to implement pipes so that the MEGA65 OS architecture can be implemented incrementally. | byte-stream blocking, EOF, close and cleanup semantics support shell pipelines |
| US-030 | IPC | Must | Backlog | L | US-023 | 9,10 | As a project contributor, I want to implement heterogeneous wait sets so that the MEGA65 OS architecture can be implemented incrementally. | threads wait on ports, pipes, timers, devices, children and later sockets without polling |
| US-031 | IPC | Should | Backlog | L | US-019, US-023 | 10 | As a project contributor, I want to implement mutexes, semaphores and events so that the MEGA65 OS architecture can be implemented incrementally. | blocking synchronization works with priority inheritance for realtime mutex contention |
| US-032 | Security | Must | Backlog | L | US-023 | 11 | As a project contributor, I want to implement capability-based authority so that the MEGA65 OS architecture can be implemented incrementally. | unauthorized object/device operations fail and authority can be explicitly granted |
| US-033 | Security | Should | Backlog | M | US-032 | 11 | As a project contributor, I want to implement SYSTEM, user and service principals so that the MEGA65 OS architecture can be implemented incrementally. | processes/objects carry principals and services can run below SYSTEM privilege |
| US-034 | Sessions | Should | Backlog | M | US-033 | 11 | As a project contributor, I want to implement session objects so that the MEGA65 OS architecture can be implemented incrementally. | local/remote session processes can be grouped and reclaimed on logout |
| US-035 | Devices | Must | Backlog | L | US-023, US-032 | 12 | As a project contributor, I want to implement the /dev namespace so that the MEGA65 OS architecture can be implemented incrementally. | named device nodes open to capability-checked handles |
| US-036 | Devices | Must | Backlog | L | US-035 | 12 | As a project contributor, I want to define common device operations so that the MEGA65 OS architecture can be implemented incrementally. | representative devices implement applicable open/read/write/seek/ioctl/map/readiness semantics |
| US-037 | Devices | Should | Backlog | L | US-035 | 12,15 | As a project contributor, I want to load drivers on demand so that the MEGA65 OS architecture can be implemented incrementally. | opening an unserved device loads its module and publishes nodes |
| US-038 | Devices | Should | Backlog | L | US-032, US-035 | 12,20 | As a project contributor, I want to support exclusive hardware capabilities so that the MEGA65 OS architecture can be implemented incrementally. | authorized apps can acquire/release VIC-like resources while OS stays scheduled |
| US-039 | Storage | Must | Backlog | L | US-035, US-036 | 12,13 | As a project contributor, I want to separate controller, block, filesystem and mount layers so that the MEGA65 OS architecture can be implemented incrementally. | raw block device remains usable independently of mounted filesystem |
| US-040 | VFS | Must | Backlog | XL | US-039 | 13 | As a project contributor, I want to implement VFS path and file-handle operations so that the MEGA65 OS architecture can be implemented incrementally. | open/read/write/seek/close operate through mounted filesystem with authority checks |
| US-041 | VFS | Should | Backlog | L | US-040, US-011 | 13 | As a project contributor, I want to map file ranges as memory objects so that the MEGA65 OS architecture can be implemented incrementally. | clean pages discard/reload and dirty mappings follow writeback rules |
| US-042 | Executable Format | Must | Backlog | XL | US-011 | 14 | As a project contributor, I want to implement the .mex extent container so that the MEGA65 OS architecture can be implemented incrementally. | validated format represents CODE/RODATA/DATA/BSS/TLS/ZEROPAGE and extent metadata |
| US-043 | Executable Format | Must | Backlog | L | US-042 | 14 | As a project contributor, I want to encode imports, exports and relocations so that the MEGA65 OS architecture can be implemented incrementally. | runtime binding resolves symbols and distinguishes near/far relocation semantics |
| US-046 | Binder | Must | Backlog | XL | US-042, US-023 | 15 | As a project contributor, I want to implement the runtime binder so that the MEGA65 OS architecture can be implemented incrementally. | binder creates objects, resolves imports, relocates, maps and loads dependencies using current state |
| US-094 | Security Decision | Must | Backlog | L | US-090 | 9,11 | As an OS architect, I want to choose capability representation and delegation rules so that implementation is not built on an unresolved architectural assumption. | Capability storage, inheritance, duplication, transfer, attenuation and revocation semantics are selected and captured in an ADR. |
| US-095 | Security Decision | Should | Backlog | M | US-096 | 11,13 | As an OS architect, I want to define filesystem ownership and permission semantics so that implementation is not built on an unresolved architectural assumption. | UID/principal representation, permission bits/ACL scope, SYSTEM behavior, service identities and default single-user behavior are specified. |
| US-096 | Session Decision | Should | Backlog | L | US-097 | 7,11,18,19 | As an OS architect, I want to define sessions, login and process-group/job-control relationships so that implementation is not built on an unresolved architectural assumption. | Local and remote login lifecycle, controlling TTY, session/process groups, logout cleanup and persistent exceptions are specified. |
| US-097 | Executable Decision | Must | Backlog | L | US-042 | 14 | As an OS architect, I want to freeze the .mex container header and extent-directory binary layout so that implementation is not built on an unresolved architectural assumption. | Field widths, endianness, alignment, versioning, bounds validation and required extent metadata are specified byte-for-byte. |
| US-100 | Executable Decision | Must | Backlog | M | US-098 | 4,14 | As an OS architect, I want to define the boot_required .mex subset so that implementation is not built on an unresolved architectural assumption. | Exactly which headers/extents/relocations/compression/integrity features bootstrap must understand are frozen and validated against bootstrap size goals. |
| US-103 | IPC Decision | Must | Backlog | M | US-090 | 10 | As an OS architect, I want to define message-port payload and handle-transfer limits so that implementation is not built on an unresolved architectural assumption. | Inline size, queue limits, ownership, blocking/backpressure, reply semantics and handle grant/transfer behavior are specified. |
| US-104 | IPC Decision | Must | Backlog | M | US-090 | 9,10 | As an OS architect, I want to define wait-set readiness and wake semantics so that implementation is not built on an unresolved architectural assumption. | Level/edge behavior, multiple-ready ordering, cancellation, timeout and object-close behavior are specified. |
