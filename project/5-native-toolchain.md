# 5 - Native Toolchain

14 work items imported from `MEGA65_OS_User_Stories.xlsx`.

| ID | Epic | Priority | Status | Estimate | Dependencies | Chapters | Work item | Acceptance criteria |
|---|---|---|---|---|---|---|---|---|
| US-044 | Executable Format | Should | Backlog | M | US-042 | 14,15 | As a project contributor, I want to encode dependency/call placement metadata so that the MEGA65 OS architecture can be implemented incrementally. | weighted call edges, dependencies, coexistence and residency hints survive into .mex |
| US-045 | Linker | Must | Backlog | XL | US-042, US-043 | 15 | As a project contributor, I want to build a development linker that forms extents so that the MEGA65 OS architecture can be implemented incrementally. | linker groups code/data, emits windows/cross-extent calls and writes valid .mex |
| US-047 | Binder | Must | Backlog | XL | US-046, US-006 | 6,15 | As a project contributor, I want to implement fixed logical windows and overlay calls so that the MEGA65 OS architecture can be implemented incrementally. | nested far calls map callee canonical window and restore caller mapping |
| US-048 | Binder | Must | Backlog | L | US-046, US-014 | 5,15 | As a project contributor, I want to demand-load executable extents so that the MEGA65 OS architecture can be implemented incrementally. | untouched code consumes no resident page and clean code reloads transparently |
| US-050 | Compiler | Must | Backlog | XL | US-049, US-013, US-047 | 6,16 | As a project contributor, I want to add native near/far pointer and far-function types so that the MEGA65 OS architecture can be implemented incrementally. | compiler lowers defined 16-bit near and 64-bit far ABIs correctly |
| US-051 | Compiler | Should | Backlog | XL | US-050, US-016 | 16,24 | As a project contributor, I want to optimize dense far-memory loops so that the MEGA65 OS architecture can be implemented incrementally. | compiler hoists mapping/pinning and handles 8 KiB boundaries with reference-equivalent results |
| US-052 | Compiler | Should | Backlog | L | US-003, US-050 | 16,24 | As a project contributor, I want to lower profitable loops to DMAgic so that the MEGA65 OS architecture can be implemented incrementally. | cost model chooses DMA only when beneficial and preserves semantics |
| US-053 | Compiler | Should | Backlog | L | US-050, US-045 | 16 | As a project contributor, I want to emit call/access metadata for the linker so that the MEGA65 OS architecture can be implemented incrementally. | object output preserves call graph, weights/hints, data dependencies and ZP/TLS needs |
| US-091 | ABI Decision | Must | Backlog | L | US-088 | 6,15,16 | As an OS architect, I want to define the far-call and mapping-stack abi so that implementation is not built on an unresolved architectural assumption. | Call frame, canonical window transition, nested calls, unwind/return behavior, interrupt interaction and failure paths are specified with executable examples. |
| US-098 | Executable Decision | Must | Backlog | L | US-098 | 14,15 | As an OS architect, I want to define .mex relocation types and symbol binding rules so that implementation is not built on an unresolved architectural assumption. | NEAR16/FAR64/FARCALL and any additional relocations have exact encoding, overflow/error behavior and linker/binder ownership defined. |
| US-099 | Executable Decision | Should | Backlog | M | US-098 | 14,15,16 | As an OS architect, I want to define .mex dependency, call-graph and placement metadata encoding so that implementation is not built on an unresolved architectural assumption. | Stable binary representation, optionality/versioning, weights/hints and unknown-field handling are specified. |
| US-101 | Linker Decision | Must | Backlog | L | US-091, US-098 | 6,15 | As an OS architect, I want to select canonical logical execution windows and coexistence rules so that implementation is not built on an unresolved architectural assumption. | Candidate 64 KiB layouts are compared; reserved kernel gateway and code/data windows are chosen with examples of overlapping extents. |
| US-102 | Linker Decision | Should | Backlog | L | US-101 | 15,24 | As an OS architect, I want to define initial weighted-graph extent placement heuristic so that implementation is not built on an unresolved architectural assumption. | Inputs, constraints, deterministic algorithm and fallback behavior are documented sufficiently to implement the first linker. |
| US-109 | Compiler Decision | Should | Backlog | L | US-088, US-107 | 16 | As an OS architect, I want to define phase-2 compiler extension syntax and semantics so that implementation is not built on an unresolved architectural assumption. | Near/far pointers, far functions, ZP/TLS, residency and OS-specific attributes have language spelling, type rules and ABI lowering defined. |
