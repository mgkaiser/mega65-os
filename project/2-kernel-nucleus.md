# 2 - Kernel Nucleus

24 work items imported from `MEGA65_OS_User_Stories.xlsx`.

| ID | Epic | Priority | Status | Estimate | Dependencies | Chapters | Work item | Acceptance criteria |
|---|---|---|---|---|---|---|---|---|
| US-010 | Memory | Must | Backlog | M | US-001 | 5 | As a project contributor, I want to implement an 8 KiB page allocator so that the MEGA65 OS architecture can be implemented incrementally. | pages allocate/free safely and track tier, state and ownership |
| US-011 | Memory | Must | Backlog | L | US-010 | 5 | As a project contributor, I want to implement arbitrary-sized memory objects so that the MEGA65 OS architecture can be implemented incrementally. | logical page slots are independent of physical contiguity |
| US-012 | Memory | Must | Backlog | M | US-010, US-011 | 5,17 | As a project contributor, I want to provide near heap backing for libc so that the MEGA65 OS architecture can be implemented incrementally. | libc requests mapped heap pages and suballocates 16-bit-pointer objects |
| US-013 | Memory | Must | Backlog | L | US-011 | 5,6 | As a project contributor, I want to implement resizable far memory objects so that the MEGA65 OS architecture can be implemented incrementally. | objects grow/shrink without moving retained logical offsets; pages may commit lazily |
| US-014 | Memory | Must | Backlog | M | US-011 | 5 | As a project contributor, I want to support discardable clean file-backed pages so that the MEGA65 OS architecture can be implemented incrementally. | evicted clean pages rematerialize from authoritative backing |
| US-015 | Memory | Should | Backlog | L | US-011 | 5 | As a project contributor, I want to support anonymous dirty paging so that the MEGA65 OS architecture can be implemented incrementally. | dirty anonymous data survives Fast/Attic/swap migration |
| US-016 | Memory | Should | Backlog | L | US-003, US-011 | 5,24 | As a project contributor, I want to implement mapping-versus-promotion policy so that the MEGA65 OS architecture can be implemented incrementally. | policy can choose mapping or synchronous DMA promotion from measurable costs |
| US-017 | Memory | Could | Backlog | L | US-011 | 5,24 | As a project contributor, I want to implement heat/cost-aware replacement so that the MEGA65 OS architecture can be implemented incrementally. | replacement considers recency, frequency, dirty/reload cost, pins and dependency heat |
| US-018 | Threads & Scheduler | Must | Backlog | M | US-002 | 7,8 | As a project contributor, I want to implement complete thread contexts so that the MEGA65 OS architecture can be implemented incrementally. | registers, private stack/ZP, TLS and scheduler state restore exactly |
| US-019 | Threads & Scheduler | Must | Backlog | L | US-004, US-018 | 8 | As a project contributor, I want to implement timer-driven preemption so that the MEGA65 OS architecture can be implemented incrementally. | two CPU-bound C threads make progress through thousands of preemptions |
| US-020 | Threads & Scheduler | Must | Backlog | M | US-018 | 7 | As a project contributor, I want to create a main thread with each process so that the MEGA65 OS architecture can be implemented incrementally. | process creation atomically establishes process state and runnable main thread |
| US-021 | Threads & Scheduler | Should | Backlog | L | US-019 | 8 | As a project contributor, I want to implement REALTIME/EXCLUSIVE/NORMAL/BACKGROUND scheduling so that the MEGA65 OS architecture can be implemented incrementally. | class ordering works and EXCLUSIVE consumes non-realtime CPU without disabling OS |
| US-022 | Interrupts | Must | Backlog | L | US-004, US-019 | 8,12 | As a project contributor, I want to implement IRQ registration and irq_wait so that the MEGA65 OS architecture can be implemented incrementally. | driver claims IRQ, sleeps, wakes on event, and releases ownership safely |
| US-055 | C Runtime | Must | Backlog | L | US-013, US-047 | 16,17 | As a project contributor, I want to define stable far-memory/map/far-call runtime primitives so that the MEGA65 OS architecture can be implemented incrementally. | reference APIs work before compiler optimization and remain valid lowering targets |
| US-083 | Memory Decision | Must | Ready | M | US-079 | 5,6,16 | As an OS architect, I want to confirm 8 kib as the kernel page and mapping quantum so that implementation is not built on an unresolved architectural assumption. | Alternatives are compared against hardware mapping, fragmentation, metadata, paging and compiler behavior; decision is captured in an ADR and Chapters 5/6/16 updated. |
| US-084 | Memory Decision | Must | Backlog | M | US-084 | 5 | As an OS architect, I want to define reserve, commit and first-touch semantics for memory objects so that implementation is not built on an unresolved architectural assumption. | Behavior for oversized reservations, allocation failure, first write/read, zero-fill and accounting is specified with API examples and failure cases. |
| US-085 | Memory Decision | Should | Backlog | L | US-085 | 5 | As an OS architect, I want to define swap backing and on-storage format so that implementation is not built on an unresolved architectural assumption. | Backing location, page identity, allocation, reclaim, crash expectations and interaction with file-backed pages are decided and documented. |
| US-086 | Memory Decision | Must | Backlog | L | US-084 | 5,15 | As an OS architect, I want to define pinning rules and pager deadlock prevention so that implementation is not built on an unresolved architectural assumption. | Which kernel paths may pin, maximum/recursive pin behavior, pager-resident dependencies and failure handling are specified and reviewed against boot/storage paths. |
| US-087 | Memory Decision | Should | Backlog | M | US-084 | 5,24 | As an OS architect, I want to define the initial page-replacement scoring model so that implementation is not built on an unresolved architectural assumption. | Inputs, weights/defaults, protected classes and observability counters are specified; model can be implemented without relying on undefined policy. |
| US-088 | ABI Decision | Must | Backlog | L | US-084 | 6 | As an OS architect, I want to select the 64-bit far-pointer bit layout so that implementation is not built on an unresolved architectural assumption. | Object identity, generation/stale-pointer protection, offset range, attribute bits and reserved/version bits are compared; one representation is frozen in an ADR. |
| US-089 | ABI Decision | Must | Backlog | M | US-088 | 6 | As an OS architect, I want to define far-pointer bounds and stale-pointer behavior so that implementation is not built on an unresolved architectural assumption. | Dereference after object resize/free/reuse and out-of-range arithmetic have specified fault/error behavior; debugger/runtime implications documented. |
| US-090 | ABI Decision | Must | Ready | L | US-088, US-089 | 6,16,17 | As an OS architect, I want to define the far dereference map/pin/unmap abi so that implementation is not built on an unresolved architectural assumption. | Reference C APIs, lifetime rules, nesting, errors and 8 KiB boundary behavior are specified sufficiently for libc and Phase-1 compiler use. |
| US-092 | Process Decision | Must | Ready | L | US-079, US-080 | 6,7,8 | As an OS architect, I want to freeze process/thread context and initial address-space layout so that implementation is not built on an unresolved architectural assumption. | Required mapped regions, kernel gateway, stack/ZP/TLS placement, canonical windows and process-start state are specified. |
| US-093 | Scheduler Decision | Should | Backlog | M | US-081 | 8 | As an OS architect, I want to define scheduling-class semantics and starvation rules so that implementation is not built on an unresolved architectural assumption. | REALTIME, EXCLUSIVE, NORMAL and BACKGROUND ordering, quanta, preemption and starvation protections are specified and test scenarios documented. |
